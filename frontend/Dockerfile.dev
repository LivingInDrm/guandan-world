# 开发环境 Dockerfile - Vite Dev Server
FROM node:20-alpine

WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY package*.json ./

# 安装依赖
RUN npm install

# 暴露 Vite 端口
EXPOSE 5173

# 启动 Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

